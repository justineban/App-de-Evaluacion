version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: student-eval-app
    ports:
      - "8081:8081"     # Metro bundler
      - "19000:19000"   # Expo DevTools
      - "19001:19001"   # Expo DevTools (secure)
      - "19002:19002"   # Expo DevTools (web)
      - "19006:19006"   # Expo web
    volumes:
      # Monta el código fuente para hot reload
      - .:/app
      # Excluye node_modules para usar los del contenedor
      - /app/node_modules
      # Excluye .git para mejor rendimiento
      - /app/.git
    environment:
      - NODE_ENV=development
      - EXPO_NO_TELEMETRY=1
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
      - REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.1
    stdin_open: true
    tty: true
    command: sh -c "npx expo start --host lan --clear"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
